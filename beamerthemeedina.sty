\mode<presentation>

\RequirePackage{tikz}
\usetikzlibrary{shapes.geometric,calc,shadows}
\RequirePackage{pgf}
\RequirePackage{graphicx}
\RequirePackage[T1]{fontenc}
\RequirePackage{tcolorbox}
\tcbuselibrary{skins}
\RequirePackage{enumitem}

% Theme options
%
% [sectionpage=<on|none>] — section divider pages. Default: on
%
% [agendastyle=<plain|pattern>] — agenda/section page background. Default: plain
%
% [numbering=<fraction|number|progressbar|none>] — slide numbering style. Default: fraction
%   fraction — i/N  (slide i of N, excluding title page)
%   number   — slide number only
%   progressbar — bottom progress bar showing i/N
%   none     — no slide numbering
\DeclareOptionBeamer{sectionpage}{\def\ed@sectionpage{#1}}
\DeclareOptionBeamer{agendastyle}{\def\ed@agendastyle{#1}}
\DeclareOptionBeamer{numbering}{\def\ed@numbering{#1}}
\ExecuteOptionsBeamer{sectionpage=off,agendastyle=pattern,numbering=fraction}
\ProcessOptionsBeamer

% Settings
\useinnertheme{edina}
\useoutertheme{edina}
\usecolortheme{edina}
\usefonttheme{edina}

% Let translated Beamer strings (for example \appendixname) become plain text
% in PDF bookmarks to avoid hyperref warnings during compilation.
\pdfstringdefDisableCommands{%
  \def\translate#1{#1}%
}

% Apply color option: if [color=X] was given, redefine edprimary and
% auto-generate all variants via xcolor mixing. Must run inline (before
% \AtBeginDocument) so that the pattern saveboxes pick up the right colors.
%
% Lighter variants: edprimaryN   = edprimary!N!white   (N = 10, 20, 30, 40)
% Darker variants:  edprimary1N0 = edprimary!(100-N*10)!black  (N = 1,2,3,4)
%   e.g. edprimary110 = edprimary!90!black  (10% black mixed in)
%        edprimary140 = edprimary!60!black  (40% black mixed in)
% \ifx\ed@coloropt\@empty\else
%   \def\ed@applyprimarycolor#1{%
%     \colorlet{edprimary}{#1}%
%     \colorlet{edprimary10}{edprimary!10!white}%
%     \colorlet{edprimary20}{edprimary!20!white}%
%     \colorlet{edprimary30}{edprimary!30!white}%
%     \colorlet{edprimary40}{edprimary!40!white}%
%     \colorlet{edprimary110}{edprimary!90!black}%
%     \colorlet{edprimary120}{edprimary!80!black}%
%     \colorlet{edprimary130}{edprimary!70!black}%
%     \colorlet{edprimary140}{edprimary!60!black}%
%   }%
%   \expandafter\ed@applyprimarycolor\expandafter{\ed@coloropt}%
% \fi

% Apply the pattern option immediately after the inner theme has defined all
% commands. This must NOT be deferred to \AtBeginDocument — the inner theme
% registers its own \AtBeginDocument to populate the pattern saveboxes, and
% that hook must see the already-switched \bgpattern.
%
% \bgpatternfacet is the only pattern with no color argument; all others
% take edprimary for \bgpattern and a color argument for \bgpatternblock.
% \def\ed@tmp{facet}\ifx\ed@pattern\ed@tmp
%   \renewcommand{\bgpattern}{\bgpatternfacet}%
%   \renewcommand{\bgpatternblock}[1]{\bgpatternblockfacet{#1}}%
% \fi
% \def\ed@tmp{facetrand}\ifx\ed@pattern\ed@tmp
%   \renewcommand{\bgpattern}{\bgpatternfacetrand{edprimary}}%
%   \renewcommand{\bgpatternblock}[1]{\bgpatternfacetrand{#1}}%
% \fi
% \def\ed@tmp{triangle}\ifx\ed@pattern\ed@tmp
%   \renewcommand{\bgpattern}{\bgpatterntriangle{edprimary}}%
%   \renewcommand{\bgpatternblock}[1]{\bgpatterntriangle{#1}}%
% \fi
% \def\ed@tmp{triangleiso}\ifx\ed@pattern\ed@tmp
%   \renewcommand{\bgpattern}{\bgpatterntriangleiso{edprimary}}%
%   \renewcommand{\bgpatternblock}[1]{\bgpatterntriangleiso{#1}}%
% \fi
% \def\ed@tmp{trianglemesh}\ifx\ed@pattern\ed@tmp
%   \renewcommand{\bgpattern}{\bgpatterntrianglemesh{edprimary}}%
%   \renewcommand{\bgpatternblock}[1]{\bgpatterntrianglemesh{#1}}%
% \fi
% \def\ed@tmp{squares}\ifx\ed@pattern\ed@tmp
%   \renewcommand{\bgpattern}{\bgpatternsquares{edprimary}}%
%   \renewcommand{\bgpatternblock}[1]{\bgpatternsquares{#1}}%
% \fi
% \def\ed@tmp{circles}\ifx\ed@pattern\ed@tmp
%   \renewcommand{\bgpattern}{\bgpatterncircles{edprimary}}%
%   \renewcommand{\bgpatternblock}[1]{\bgpatterncircles{#1}}%
% \fi
% \def\ed@tmp{hexraise}\ifx\ed@pattern\ed@tmp
%   \renewcommand{\bgpattern}{\bgpatternhexraise{edprimary}}%
%   \renewcommand{\bgpatternblock}[1]{\bgpatternhexraise{#1}}%
% \fi
% \def\ed@tmp{hexrand}\ifx\ed@pattern\ed@tmp
%   \renewcommand{\bgpattern}{\bgpatternhexrand{edprimary}}%
%   \renewcommand{\bgpatternblock}[1]{\bgpatternhexrand{#1}}%
% \fi
% \def\ed@tmp{network}\ifx\ed@pattern\ed@tmp
%   \renewcommand{\bgpattern}{\bgpatternnetwork{edprimary}}%
%   \renewcommand{\bgpatternblock}[1]{\bgpatternnetwork{#1}}%
% \fi
% \def\ed@tmp{quilt}\ifx\ed@pattern\ed@tmp
%   \renewcommand{\bgpattern}{\bgpatternquilt{edprimary}}%
%   \renewcommand{\bgpatternblock}[1]{\bgpatternquilt{#1}}%
% \fi
% \def\ed@tmp{plain}\ifx\ed@pattern\ed@tmp
%   \renewcommand{\bgpattern}{\bgpatternplain{edprimary}}%
%   \renewcommand{\bgpatternblock}[1]{\bgpatternplain{#1}}%
% \fi

\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{blocks}[shadow=false]

\mode<all>
